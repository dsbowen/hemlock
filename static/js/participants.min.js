$(document).ready(function(){
    var socket = io.connect($SOCKET_URL);
    socket.on("status-update", function(e){
        $("#completed").text(e.completed);
        $("#in_progress").text(e.in_progress);
        $("#timed_out").text(e.timed_out);
        $("#total").text(e.total);
    });
});